info:
  name: add
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:5000/add_product
  body:
    type: json
    data: |2-
       {
         "name": "surfboard",
         "price": 50,
         "quantity": 1
       }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
